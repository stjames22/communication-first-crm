# Tasks

Keep this list short. Add only the next small work items.

- Run full workflow smoke test after each feature change.
- Improve contact creation UX after real-user feedback from demo testing.
- Expand automated regression coverage around quote lifecycle and timeline events.
- Add production auth plan after v1 workflow is proven.
- Replace provider placeholders only when real provider credentials and webhook requirements are known.
